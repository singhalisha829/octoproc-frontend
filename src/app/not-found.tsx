import React from "react";

const NotFoundPage = () => {
  return (
    <div className="flex items-center justify-center text-4xl font-medium uppercase h-full">
      oops.. the page that you were looking for is not available.
    </div>
  );
};

export default NotFoundPage;
